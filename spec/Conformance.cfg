\* The width is the ASSUME block: TLC proves every mapping value on Example.
INIT Init
NEXT Next

CONSTANTS
    Submission = {"s1", "s2", "s3"}
    Runner = {"j1"}
    Verdict = {"AC", "WA", "CE"}
    MaxAttempts = 2
    MaxResolves = 1
